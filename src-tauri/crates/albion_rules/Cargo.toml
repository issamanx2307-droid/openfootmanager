[package]
name = "albion_rules"
version = "0.1.0"
edition = "2024"
description = "Project Albion versioned competition/business rulesets (data/rulesets/*.yaml)."

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
serde_yaml = "0.9"

[dev-dependencies]

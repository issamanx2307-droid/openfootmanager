[package]
name = "albion_protocol"
version = "0.1.0"
edition = "2024"
description = "Project Albion wire protocol: versions, commands, events, error codes."

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
uuid = { version = "1", features = ["v4", "serde"] }

[dev-dependencies]
